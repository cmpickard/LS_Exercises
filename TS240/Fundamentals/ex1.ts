// Create three variables, each with an explicit type annotation.
// Set them to a string, a number, and a boolean value.

// Expected outcome:

// A variable with a string value, annotated with type string.
// A variable with a number value, annotated with type number.
// A variable with a boolean value, annotated with type boolean.

let var1: string = '';
let var2: number = 1;
let var3: boolean = true;
console.log(var1, var2, var3);