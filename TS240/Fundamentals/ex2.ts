// Create an array called colors that can only hold strings.
// Initialize it with a few colors of your choice.

// Expected outcome:

// An array that allows only strings.

let strArr: Array<string> = ['red', 'blue'];
console.log(strArr);