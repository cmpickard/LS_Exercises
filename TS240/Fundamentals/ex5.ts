// Define a function logMessage that takes a string argument and logs it
// to the console, but doesn't return anything.

// Expected outcome:

// The function logs output to the console but returns void.

function logMessage(arg: string): void {
  console.log(arg);
}

logMessage('hello');